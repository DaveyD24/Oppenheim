using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PatrolBot : EnemyBot
{
    
    

    // Start is called before the first frame update
    void Start()
    {
        maxHealth = 100;
        maxHealth = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
